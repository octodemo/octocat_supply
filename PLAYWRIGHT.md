# Playwright

Playwright is used for end-to-end testing of the frontend application.

## Usage
- Write E2E tests in frontend/tests/e2e
- Run tests with `make test` or `cd frontend && npx playwright test`
- See .github/copilot-instructions.md for agent orchestration
